#include <stdio.h>

/*
    this is the basic skeleton for a C code, standalone, as a script.
    with gcc (windows compiled, but this one i guess is pretty portable)
    will generate an exe and when the exe is executed will show a message 
    in the console.

    consider it your standard hello world example
*/

int main(){
   int value = 101; 
   printf("this is a value from a var %d  \n\n", value);
}